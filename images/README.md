# Results

This directory contains generated model-evaluation figures used by the project README.

Run the training script with the dataset available locally to regenerate them under `artifacts/figures/`.

Reference visual categories included in the project design:
- model comparison / PR-AUC
- precision-recall curves
- threshold trade-offs
- confusion matrix
- feature importance
- correlation and EDA plots
